<UserControl x:Class="McSntt.Views.UserControls.FrontPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="600">
    <Grid>
        <StackPanel Orientation="Horizontal">
            <StackPanel Width="300">
                <TextBlock Text="Velkommen " />
            </StackPanel>
            <StackPanel>
                <DataGrid CanUserAddRows="False" x:Name="UpcommingTripsDataGrid" AutoGenerateColumns="False" HorizontalAlignment="Left" VerticalAlignment="Top" Height="180" Width="300" MaxHeight="180">
                    <DataGrid.Columns>
                        <DataGridTextColumn Binding="{Binding Boat}" Header="Boat"/>
                        <DataGridTextColumn Binding="{Binding MemberId}" Header="ID" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>

        </StackPanel>
    </Grid>
</UserControl>
